using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "Stat Rune", menuName = "sykcorSystems/Items/Runes", order = 1)]
public class StatRune : RuneData
{
    // This class may need to be reworked, hard to align with no type reference, alternatively, playerstats contains on stats with typereference so link is direct
    // Start is called before the first frame update
}
